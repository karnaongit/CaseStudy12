apiVersion: v1
kind: Service
metadata:
	name: spark-analytics-jupyter-service
	namespace: default
spec:
	selector:
		app: financial-analytics
	ports:
		- protocol: TCP
			port: 8888
			targetPort: 8888
			nodePort: 30088
	type: NodePort
